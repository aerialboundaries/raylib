#include "blocks.h"
#include "position.h"

/* 各ミノの回転状態ごとのローカル相対座標の定義 */
const Position block_shapes[8][4][4] = {
    /* 0番（不使用：空のブロック用） */
    [0] = {{{0, 0}}},

    /* Tミノの例 */
    [T_BLOCK] =
        {
            {{0, 1}, {1, 0}, {1, 1}, {1, 2}}, /* 回転状態 0 */
            {{0, 1}, {1, 1}, {1, 2}, {2, 1}}, /* 回転状態 1 */
            {{1, 0}, {1, 1}, {1, 2}, {2, 1}}, /* 回転状態 2 */
            {{0, 1}, {1, 0}, {1, 1}, {2, 1}}  /* 回転状態 3 */
        }
    /* 同様にしてI_BLOCK, O_BLOCKなどもここに定義していきます */
};
